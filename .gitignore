# ── Environment files (contain secrets) ──
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ── Generated runtime config (compiled from .env) ──
js/config.js

# ── Node modules (if any) ──
node_modules/

# ── OS / Editor artifacts ──
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# ── Vercel local cache ──
.vercel/
